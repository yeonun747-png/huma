import { AppShell } from '@/components/dashboard/app-shell';
import { LanguagesView } from '@/components/pages/workspace-views';

export default function LanguagesPage() {
  return (
    <AppShell>
      <LanguagesView />
    </AppShell>
  );
}
