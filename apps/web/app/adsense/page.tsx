import { AppShell } from '@/components/dashboard/app-shell';
import { AdsenseView } from '@/components/pages/workspace-views';

export default function AdsensePage() {
  return (
    <AppShell>
      <AdsenseView />
    </AppShell>
  );
}
