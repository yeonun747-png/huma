# C-Rank 직결 실폰 — 슬롯6·7 ADB serial (adb devices -l)
# 복사: sudo cp phone-crank-slots.example.conf /etc/huma/phone-crank-slots.conf
#
# USB 직결 포트를 바꾸면 serial만 유지하면 됨 (iface는 restore가 자동 탐색)
6=RF9R202012L
7=5200abbbee7474b5